package kr.code6150.database;

public class Main {

    public static void main(String[] args) {

//        DbManager dbm = DbManager.getInstance();
//
//        //번호를 자동으로 설정해주는 옵션 [primary key auto_increment] -> 반드시, primary key 여야만 한다.
//        // 1. 아이디 - 비번            [unique]
//        // 2. 아이디 - 비번
//        // 3. 아이디 - 비번
//
//        // create table if not exists `테이블명` (`컬럼명` text)
//
//        dbm.execute("create table if not exists `info` (`num` text primary key auto_increment, `id` char(24) unique not null, `pw` text not null)");
//
//
    }

}